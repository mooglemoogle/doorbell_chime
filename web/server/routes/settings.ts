import { Router } from "express";

export default (router: Router) => {
    router.get('/api/settings/running', (req, res) => {

    });
    router.post('/api/settings/running/:new_val', (req, res) => {
        
    });
}